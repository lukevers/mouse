[servers]

    [servers.fc00]

        nick = "mouse"
        user = "mouse"
        name = "mouse"

        host = "irc.fc00.io"
        port = 6667
        tls = false
        reconnect = true
        debug = true

        channels = [ "#lukevers", "#mouse", "#mice" ]

        [servers.fc00.plugins.javascript]
            enabled = true
            folders = [ "contrib/scripts/javascript/" ]
            pattern = "*.js"
            events = [ "PRIVMSG" ]
